/**
 * @typedef {{ totalAssets:number, totalLiabilities:number, netWorth:number, deltaPct:number, currency:string }} DashboardSnapshot
 * @typedef {{ date:string, netWorth:number }} TrendPoint
 * @typedef {{ category:string, pct:number, amount:number }} AllocationSlice
 * @typedef {{ id:string, message:string, level:'high'|'medium'|'low' }} AlertItem
 * @typedef {{ inflow:number, outflow:number }} CashflowSummary
 */

export {};
