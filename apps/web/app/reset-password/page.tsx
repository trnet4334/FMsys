import { Suspense } from 'react';
import { ResetPasswordPanel } from '../../src/components/auth/reset-password-panel';

export default function ResetPasswordPage() {
  return <Suspense><ResetPasswordPanel /></Suspense>;
}
